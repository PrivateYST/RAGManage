# 方案目录恢复记录

2026-09-17：执行目录移动后，Claude、DeepSeek、Codex 三个方案目录未出现在预期目标位置。已暂停工程搭建，优先恢复。具体丢失原因尚未查明，不能将其归因于用户手动移动。

从原会话 `01a0ae03-8d55-7052-b084-6dd5373bb908` 的本地完整会话记录恢复：

- Claude：4 份，使用当时 Get-Content -Raw 的完整输出。
- DeepSeek：5 份，使用当时 Get-Content -Raw 的完整输出。
- Codex：4 份，使用当时创建文件的原始补丁。
- 最终方案：7 份，从原目录逐文件移动，未重建正文。

恢复目标统一为根目录 `md/`。已核对 20 份 Markdown 文件数量、正文无 Unicode 替换字符、文档内相对 Markdown 链接有效。
13 份恢复文件逐一与恢复来源文本比对；来源行号及恢复后 SHA256 见 `document-recovery.json`。

恢复的是原记录中的正文，不是重新生成的方案。未保留丢失前文件哈希，无法承诺 BOM、末尾换行及文件时间戳与原文件逐字节一致。
原 `最终可行方案md` 目录已空，但仍受进程占用，暂保留空目录，不强制结束其他应用。
